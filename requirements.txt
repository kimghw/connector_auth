# Azure Authentication Module Requirements
aiohttp>=3.8.0
python-dotenv>=0.19.0
cryptography>=3.4.8
psutil>=5.9.0
pydantic>=2.0.0
PyYAML>=6.0

# Web Server (REST/Stream protocols)
fastapi>=0.100.0
uvicorn>=0.23.0

# MCP Proxy (for Claude Desktop remote connection)
mcp-proxy>=0.1.0

# File Conversion Libraries (for mail attachment processing)
pdfplumber>=0.10.0        # PDF text extraction
python-docx>=0.8.11       # DOCX text extraction
olefile>=0.47             # HWP processing (Linux/Windows)
openpyxl>=3.1.0           # XLSX text extraction
python-pptx>=0.6.21       # PPTX text extraction
# pyhwpx>=0.1.0           # HWP processing (Windows only, optional)